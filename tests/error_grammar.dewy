//example of a grammar that contains errors.
//should only have one rule uncommented at a time

//using a non-existent identifier
#bad1 = #never_defined;

//using symbols that aren't part of the metascannar syntax
//#bad2 = 'apple' $ % ^ 'banana';

//forgetting to end a rule
//#bad3a = 'rule that doesn't terminate'
//#bad3b = 'next rule that terminates';

//non-terminating rule at end of file
//#bad10 = 'last rule that forgets to terminate'
