# Call parentheses and coefficient multiplication keep their distinct
# precedences after removing numeric right-hand adjacency.
main=():>int64=>{
    let x:int64=3
    let square=(n:int64):>int64=>n*n
    return if 2x^2=?18 and square(2)^2=?16 42 else 1
}
