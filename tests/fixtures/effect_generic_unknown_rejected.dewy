let apply = <E:Effect>(f:():>int64 & E):>int64 & E => f()
# An unknown callback has no body from which to infer a narrower row.
unknown = (callback:():>int64):>int64 & no_effects => apply(@callback)
main = ():>int64 => 42
