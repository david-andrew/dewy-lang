let total:int64=0
CounterHandle=type of [token:int64
$__drop__
release=():>void=>{total=1}
]
dropper=():>void=>{let h=CounterHandle[42]}
main=():>int64=>{
    total=0
    dropper()
    # Implicit cleanup is a real write, including across the call boundary.
    $assert total=?0
    return 42
}
