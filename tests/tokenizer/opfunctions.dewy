# some opfunc literals
(+) (-) (*) (/) (idiv) (^) (|) (&) (nand) (and) (or) (xor)
(=) (+=) (-=) (.+) (.+=)

# example of what defining a plus reduction function might look like
sum = <T>(values:array<T>) => (+)(values...)

values = [1 2 3 4 5 6 7 8 9]

sum(values)

# sum up values as a reduce loop
x = 0
values.reduce((+=) @x)


cycleleft = @(`)(dir='left')