'regular {interpolation} in a string'
t'template ${interpolation} in a string'

$'DELIM'\
regular herestring
with {interpolation}
DELIM

$t'DELIM'\
template herestring
with ${interpolation}
DELIM

# Note, can only have one rest-of-file string per file, so uncomment the one you want to test
# $'''\
# regular rest-of-file string with {interpolation}

$t'''\
template rest-of-file string with ${interpolation}