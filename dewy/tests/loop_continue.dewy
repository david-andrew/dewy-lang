let main = ():>int64 => {
    let index:int64 = 0
    let total:int64 = 0
    loop index <? 10 {
        index += 1
        if (index % 2) =? 0 {
            continue
        }
        total += index
    }
    return total + 17
}
