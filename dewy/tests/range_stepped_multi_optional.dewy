let main = ():>int64 => {
    let total:int64 = 0
    loop i in 0,2..2 or j in 10,20..30 {
        if i is? none {
            total += 30
        } else {
            total += i
        }
    }
    return total + 10
}
