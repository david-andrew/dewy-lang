# a rest-of-file string has an opener but no closer: everything after it is the string
let header = "before the rest-of-file string"
let rest = $"""
Everything from here to the end of the file is one string,
including "quotes", 'apostrophes', {braces}, #{ what looks like a comment }#,
and the missing closing quotes.
