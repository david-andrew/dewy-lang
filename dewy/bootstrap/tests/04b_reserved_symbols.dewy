# Symbols the tokenizer knows but the parser gives no meaning yet: they tokenize
# (the bootstrap tokenizer should produce the same tokens) and stop at the parser.
let h = a <=> b        # three-way comparison
let i = a ?? b         # null-coalescing
