# (() => printl'Hello from an anonymous function!')()
export function w $main() {
@start
    call $lambda0(l $str)
    ret 0
}

function $lambda0(l %s) {
@start
    call $printf(l %s, ...)
    ret
}
data $str = { b "Hello from an anonymous function!", b 0 }