# Example program
# printl'hello world!'

data $greet = { b "Hello World!\n\0" }

export function w $__main__(l %argc, l %argv, l %envp) {
@start
        call $__write__(l $greet, l 13)
        ret 0
}