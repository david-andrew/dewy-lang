from p"lib.dewy" import (answer add Number)
import p"lib.dewy" as library

let main = ():>int64 => {
    let value:library.Number = answer
    if add(value) =? 42 and library.add(40) =? 42 {
        return 42
    }
    return 1
}
