let main = ():>uint8 => {
    let bytes:array<uint8> = [1 2 3]
    let i:int64 = 0
    loop i <? bytes.length {
        if i =? 1 {
            bytes[i] = 42
        }
        i += 1
    }
    return bytes[1]
}
