let main = ():>int64 => {
    let total:int64 = 0
    loop i in 0..1 or j in 10..12 {
        if i is? undefined {
            total += 30
        } else {
            total += i
        }
    }
    return total + 11
}
