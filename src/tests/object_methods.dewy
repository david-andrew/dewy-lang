let main = ():>int64 => {
    let o = [
        a = 10
        b = 20
        sum = ():>int64 => a + b
        add = (x:int64):>int64 => a + b + x
    ]
    let total = o.sum
    return o.add(total - 18)
}
