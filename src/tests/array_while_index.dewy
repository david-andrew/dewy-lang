let main = ():>int64 => {
    let values:array<int64> = [10 20 12]
    let i:int64 = 0
    let total:int64 = 0
    loop i <? values.length {
        total += values[i]
        i += 1
    }
    return total
}
