# Data Structures

Arrays, dictionaries, bidirectional maps, and sets are language
containers. See [Container Types](../ch03/container-types.md).

The standard library builds queues, graphs, and other structures that
are not worth a literal syntax. Prefer a language container when it
fits; reach for a library type when you need extra operations or a
different performance shape.
