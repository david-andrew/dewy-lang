# Plotting

The standard library should include common plots out of the box, including ridgeline plots and Sankey diagrams that can be built from graph data.

Animation, and whether a plot blocks the caller, are not yet determined.
