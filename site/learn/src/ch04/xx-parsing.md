# Parsing

The standard library should make it straightforward to parse from a
grammar, as easy as writing a regular expression for simple cases, and
powerful enough for a full language.

Intended directions include generating tree-sitter compatible C parsers
and hooks that language servers can use. The library surface is not yet
determined.
