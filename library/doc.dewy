# Documentation strings. `doc"…"` (usually a `"""` block) at the top of a
# module, a function body, or a type documents what follows; the compiler
# keeps nothing from it yet, so the call is a no-op.
let doc = (text:string):>void => void
