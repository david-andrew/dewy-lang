% another minimal program
% start some process, lookup it's PID, and paste it here
% $ sleep 9999 &       # to start a process
% [1] <PID of sleep>   # the process ID gets printed out on the next line
% $ ps -p <PID>        # to check the status of the process
SYSCALL_KILL = 62
SIGTERM = 15
__syscall2__(SYSCALL_KILL 33109 SIGTERM)