#!/home/david/dev/dewy-lang/dewy

% probably will just have a shebang operator since I got rid of // meaning comments
% alternatively #! could be a special hashtag identifier, and / could be the division operator. 
% just problem that the path chunks aren't valid identifiers that exist. TBD what to do exactly
printl'this code was invoked as an executable script with a shebang line!'