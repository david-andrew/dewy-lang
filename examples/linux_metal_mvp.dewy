// TODO: for scripts not in __main__, __root__ or __script__ is the implicitly defined top level function that stuff happens in
// if __main__ isn't defined, then the default one should call __root__?
//SYSCALL_EXIT = 60
//__main__ = () => {
//    __syscall1__(SYSCALL_EXIT 42)
//}

// even simpler
__main__ = () => __syscall1__(60 42)