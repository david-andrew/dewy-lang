# Hello world test for ARM64
import p"prelude_arm.udewy"

let main = ():>int => {
    let msg:int = "Hello from ARM64!\n"
    let len:int = __load__(msg + 0 - 8)
    __syscall3__(SYS_WRITE STDOUT msg len)
    return 0
}
