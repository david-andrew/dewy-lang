# Hosted C profile marker for the `c` backend.
#
# Import this module, or a module that imports it, when a program intends to
# use ordinary hosted C facilities. The C backend uses this import as a source
# level capability signal; the module intentionally exports no runtime values.
