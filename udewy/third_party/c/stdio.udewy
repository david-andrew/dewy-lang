import p"hosted.udewy"

# Minimal stdio bindings for the hosted C backend.
# Importing this module lets the C backend include <stdio.h> and route these
# symbols through typed wrappers instead of the generic raw extern fallback.

let putchar = (ch:int):>int => extern
let puts = (s:int):>int => extern
