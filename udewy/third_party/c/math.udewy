import p"hosted.udewy"

# C math capability marker for the hosted C backend.
#
# The current C backend can use this import to include <math.h> and link the
# host math library. Rich floating-point bindings will need a mixed integer/FP
# extern calling convention, so this module does not declare those APIs yet.
